<USER_REQUEST>
Subject: Enterprise Architecture Refactor: Deploying Split-Installment Database Fix and Unified 2-Tab Vehicle Operational Command Hub

Role: Principal SaaS Financial Architect & Core Full-Stack Next.js Developer

We are launching a critical full-stack refactor across `actions.ts` and `VehicleDetails.tsx`. We must eliminate a severe accounting bug where partial-payment cascades on installments shrink records in-place without generating a matching 'paid' row, causing subsequent balances to over-subtract and break dynamic revenue metrics. Concurrently, we are overhauling the vehicle profile view into a hyper-focused, 2-tab asset-centric command room, removing multi-tab fragmentation and analytical scoring bloat.

⚠️ STIPULATION: EXECUTE THE INSTALLMENT-SPLITTING DATABASE TRIGGERS AND COMPACT UI TAB MATRIX IMMEDIATELY. OUTPUT COMPLETED, TYPE-SAFE SOURCE CODE.

### 1. Backend Core: Split-Installment Financial Reconciliation (`actions.ts`)
Refactor the transactional map logic within `settleBookingTrancheCascade` and `updateBookingHistoricalDetails` to eliminate data truncation errors during partial payment collections:
* **The Installment Row-Splitting Algorithm**: When an incoming payment volume partially satisfies an unpaid installment row item (e.g., a customer pays 200 DT towards a 400 DT scheduled tranche):
  1. **Update the Existing Node**: Mutate the target unpaid installment row's amount cell to represent only the remaining outstanding balance: `New Balance = Original Amount - Paid Amount` (e.g., 200.00 DT).
  2. **Insert the Realized Node**: Concurrently insert a brand-new historical database record into `booking_installments` capturing the exact paid volume fraction: `amount = Paid Amount`, `status = 'paid'`, `due_date = original_installment.due_date`, and `paid_date = CURRENT_DATE` (May 22, 2026).
* This enforces absolute consistency across `paidInstallmentsSum` aggregates, completely eliminating negative-balance calculations. Trigger CRM `recalculateClientTrustScore` immediately upon exec
<truncated 949 bytes>
h query.
  * **Strict Vidange Automation Threshold**: Run the inline metric formula check: If `Current Odometer - Last Recorded Vidange Odometer >= 10000 km`, immediately trigger an intense flashing neon crimson alert element: `🚨 VIDANGE DUE`.

* **TAB 2: Unified Asset Timeline (Chronological Financial Spreadsheet)**
  * Embed a single, consolidated chronological data sheet rendering every transaction event generated by this specific asset (`vehicle_id`), sorted strictly by `date DESC`.
  * Columns: `Date` | `Event Type Badge` | `Description Summary` | `Cash Delta (Inflow/Outflow)`.
  * Visual Rule: Rental contract earnings show as solid green cash inflows (`+1,500.00 DT`). Mechanics garage invoices, fuel log cash outs, and insurance renewal fees render as bold crimson outflows prefixed with a negative sign (`-320.00 DT`).

### 3. Cross-Table Asset Expense Automated Syncing (`actions.ts`)
* When an operator updates mechanical repairs or advances compliance dates directly from the vehicle dashboard tab panels, the server handler must execute concurrent SQL updates, calculating the true operational deficit, and instantly append a matching entry row directly into the main database `expenses` matrix table to secure dashboard cash-health balance sheets without manual double entry overhead.

### Design System Compliance:
* Maintain strict adherence to our premium glassmorphic Obsidian base canvas accented with soft Champagne gold highlight boundary lines (`#E5C17D`).
* Lock all financial numerical cells, inputs, badges, and totals strictly to currency marker token **DT**.
* Under no circumstances can rows or widgets support real human facial photography. Enforce text initials monogram capsules for customer nodes.

Please output the fully refactored, production-ready Next.js source code blocks for `actions.ts` and `VehicleDetails.tsx`. Validate for absolute compilation readiness under `npx tsc --noEmit`.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-05-22T17:07:16+02:00.
</ADDITIONAL_METADATA>